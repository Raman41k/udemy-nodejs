version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: ".env"
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - .:/usr/src/app
    networks:
      - app-network
    command: "npm run dev"
networks:
  app-network:
    driver: bridge
